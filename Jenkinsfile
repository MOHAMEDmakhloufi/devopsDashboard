pipeline {
    agent any
    stages {
        stage("Init") {
            steps {
                nodejs(nodeJSInstallation: 'nodejs') {
                    sh "npm --version"
                }
            }
        }
        
      

    }
}
