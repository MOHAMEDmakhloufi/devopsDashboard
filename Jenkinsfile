pipeline {
    agent any
    stages {
        stage("Sonarqube analysis ") {
            steps {
                nodejs(nodeJSInstallationName: 'nodejs') {
                    sh "npm --version"
                    
                }
            }
        }
        
      

    }
}
