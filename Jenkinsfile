pipeline {
    agent any
    stages {
        stage("Init") {
            steps {
                nodejs(nodeJSInstallationName: 'nodejs') {
                    sh "npm --version"
                }
            }
        }
        
      

    }
}
